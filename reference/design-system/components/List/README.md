# List

Скруглённый вставной список; каждый дочерний элемент становится строкой.

**Потребитель даёт:** дочерние `ListItem`, опционально `striped` (зебра `surface-2`), `bare` (без фона и рамки — внутри Accordion или Panel), `label`.

- Разделители — `line`.
