# Badge

Бейдж: короткий статус или счётчик.

**Потребитель даёт:** `children` (одно-два слова или число), `tone`, `count` для счётчика.

- `neutral` — серебро, `accent` — новое или счётчик, `success`/`warning`/`danger` — утопленная плашка с цветным текстом и иконкой.
