# Checkbox

Флажок: утопленный квадрат с зелёной галочкой, вся строка — зона касания 44px.

**Потребитель даёт:** `label`, `checked`/`defaultChecked`, `onChange(checked)`, `disabled`.

- Подпись — утверждение, которое становится истинным при отметке.
- Движение: галочка выпрыгивает с поворотом.
