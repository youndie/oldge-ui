# RadioGroup

Группа переключателей «один из»: утопленный круг с акцентной точкой.

**Потребитель даёт:** `options` `{value, label, disabled?}`, `value` или `defaultValue`, `onChange`, `label` группы.

- От двух до шести вариантов, стопкой.
