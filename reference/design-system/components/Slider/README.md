# Slider

Ползунок: утопленный жёлоб с акцентной заливкой и хромированным бегунком-шаром.

**Потребитель даёт:** `value` (0..1), `onChange`, `label`, `valueText` (как показать значение).

- Нативный `input[type=range]`: клавиатура и скринридер работают без доработок.
