# Divider

Разделитель — аналог Material Divider: вытравленная двойная линия XP (тень `line` и блик `gloss`).

**Потребитель даёт:** опционально `label` — пиксельная надпись посередине («или»).

- Между группами контента. Внутри списков используйте разделители List.
